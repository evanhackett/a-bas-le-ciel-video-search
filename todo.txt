todo

deploy script to server

on initial load, show all videos in search results

investigate why progress bar for loading json works on localhost but not on gh-pages.

DONE: fixed the "results per page" dropdown and pagination layout on small screens.
The row needed 490px against ~350px on a phone. Root cause was main.js forcing
display:block on .pagination, which killed its own flex rules.

DONE: added "contains all words". On "antinatalism ecology" the three modes give
0 (exact), 483 (any) and 6 (all) results against the current dataset.

performance optimizations for search in webapp.

Progress bar for long searches.

LLM-based search. Would be cool to describe to the LLM the kind of thing you are looking for and have it recommend things.

cache (maybe using localstorage) so users don't have to download the large file every time. Need to make sure they get the updated file though if the file changes.